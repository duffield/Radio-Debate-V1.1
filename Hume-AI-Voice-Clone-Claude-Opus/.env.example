# Hume AI Voice Cloner (.env)

# Required
HUME_API_KEY=
HUME_SECRET_KEY=

# Optional: Use an existing config instead of creating a new one
# If set, the app will use this config_id for the conversation
HUME_CONFIG_ID=

# Optional: Override the voice for this session (non-destructive)
# If both HUME_CONFIG_ID and HUME_VOICE_ID are set, the app derives a new
# config that reuses the template's prompt/LLM and applies this voice_id.
HUME_VOICE_ID=
